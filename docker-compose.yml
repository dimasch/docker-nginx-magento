mysql:
 image: mysql
 environment:
  MYSQL_ROOT_PASSWORD: tmp
web:
 image: komplizierte/docker-nginx-magento
 ports:
  - "80:80"
 links:
  - mysql:db
 volumes:
  - .:/var/www/magento
 environment:
  BASE_URL: http://kmplzt.dev/